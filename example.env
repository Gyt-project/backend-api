DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=secret
DB_NAME=mydatabase
GIT_SERVER_ADDR=git.example.com

# ── Redis cache ───────────────────────────────────────────────
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=

# ── gRPC backend ──────────────────────────────────────────────
GRPC_PORT=50051
JWT_SECRET=changeme-super-secret-key-for-development

# ── API Gateway (GraphQL) ─────────────────────────────────────
GRPC_BACKEND_ADDR=localhost:50051
GATEWAY_PORT=8080
ENV=development

# ── Live PR API (WebSocket + SSE) ─────────────────────────────
LIVE_PORT=8090

